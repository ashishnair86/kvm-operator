# This rule is not used by the project kvm-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the infra.example.com.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: kvm-operator
    app.kubernetes.io/managed-by: kustomize
  name: kvmvm-editor-role
rules:
- apiGroups:
  - infra.example.com
  resources:
  - kvmvms
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - infra.example.com
  resources:
  - kvmvms/status
  verbs:
  - get
