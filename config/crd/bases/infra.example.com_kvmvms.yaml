---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kvmvms.infra.example.com
spec:
  group: infra.example.com
  names:
    kind: KVMVM
    listKind: KVMVMList
    plural: kvmvms
    singular: kvmvm
  scope: Namespaced
  versions:
  - name: v1alpha1
    served: true
    storage: true
    schema:
      openAPIV3Schema:
        type: object
        properties:

          spec:
            type: object
            properties:

              vm_name:
                type: string

              memory:
                type: integer

              vcpus:
                type: integer

              disksize:
                type: integer

              image:
                type: string

              network:
                type: string

          status:
            description: Status defines the observed state of KVMVM
            type: object
            x-kubernetes-preserve-unknown-fields: true
    subresources:
      status: {}
